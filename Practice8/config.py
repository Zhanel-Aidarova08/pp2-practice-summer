# config.py
# Database connection settings — change these to match your PostgreSQL setup

DB_CONFIG = {
    "host":     "localhost",
    "port":     5432,
    "dbname":   "phonebook_db",
    "user":     "postgres",
    "password": "your_password",   # ← замени на свой пароль
}
